#include <stdio.h>
#define swap(t,x,y) {t r = a; a = b; b = r;}
int main()
{
    int a, b;
	scanf("%d", &a);
	scanf("%d", &b);
    swap(float,a,y);
    printf("%d %d\n", a ,b);
    return 0;
}

